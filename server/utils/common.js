/**
 * content type - should be align with client's EContentType!
 * @type {{Job: number, Company: number, User: number, CV: number}}
 */
var EContentType = {
	Job: "job"
	, Company: "company"
	, User: "user"
	, CV: "cv"
}

module.exports = {
	EContentType: EContentType
}